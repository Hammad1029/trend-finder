{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Trend Finder",
            "type": "debugpy",
            "request": "launch",
            "module": "trend_finder",
            "cwd": "${workspaceFolder}",
            "python": "${workspaceFolder}/.venv/bin/python",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}